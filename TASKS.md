You are working on Nine to Survive: Your Number, in /Users/kevindriscoll/Desktop/nine to survive/. Read SESSION_LOG.md and HANDOFF.md first; they are accurate. The structure already exists and must be preserved exactly:

- ntos-game.js: the brain (NineToSurvive). Career rules, meters, WORLD_EFFECTS, verdicts. No DOM. All randomness via seeded mulberry32 on g.rngState, never bare Math.random.
- ntos-world.js: the office (NtosWorld). Real-time iso world, BFS pathing, signal queue from step(w, dt). Never touches meters. render()/screenToTile() are the only canvas code.
- index.html: the shell. Presentation and wiring only, loads the modules via script src tags with ?v= cache busters (n# for game, w# for world). Never re-implements rules.
- ntos-standalone.html: fully inlined shareable, rebuilt by build_standalone.py. Never edit it by hand.
- game-test.js (59 passing) and world-test.js (44 passing): headless tests run via `osascript -l JavaScript <file>` under JavaScriptCore. No node, no jsdom, no npm. Extend these files; do not create parallel test infrastructure.

The done-criteria for EVERY task: both test suites green, ?v= bumped on any changed module, `python3 build_standalone.py` run successfully, and a commit with message prefixed TASK N. Standing design direction from HANDOFF: if a feature could be a popup, ask whether it can be a thing in the world instead.

TASK 1 (REQUIRED): Fix the zero-path arrival bug in ntos-world.js. bfsPath returns an empty array when the actor is already on the target tile, and sendTo sets a non-idle state anyway. The movement loop in step() only processes actors with a.path.length, so an actor in state summoned, patrol, raid, or errand with an empty path never arrives and can deadlock the day (an encounter owner already adjacent to the player never fires the card, freezing nextEvent forever). Fix sendTo (or the movement loop) so an empty path triggers the same arrival handling as the arrived branch in step(). Add regression tests to world-test.js: an owner spawned adjacent to the player still fires the encounter signal; a boss patrol starting adjacent still resolves to bosspass or bosscatch.

TASK 2 (REQUIRED, the center of this session): Build a soak test. Add to game-test.js (or a new soak section in world-test.js if it fits better there, since it must drive the world too) a bot that plays full days through the real pipeline: W.newDay each morning, W.step(w, 0.1) in a loop, player held at desk via W.playerGoHome, cards resolved by applying choice index 2 and resolving, crunches auto-won via G.applyCrunch(g, true) plus W.resolveCrunch, dayover fed into G.closeDay, then G.nextDay. Run up to 200 days or until a terminal state, across at least 50 seeds. Assert per day: the clock reaches 5 PM in bounded steps (hard cap on step iterations, fail loudly if exceeded), no actor sits in a non-idle state with an empty path for more than 60 simulated seconds, and no exceptions. Fix every hang or exception this finds. Do not skim this task.

TASK 3 (REQUIRED): Add a stuck watchdog to the shell in index.html. Every 5 real seconds, if no overlay is open and world.clockMin has not advanced in 20 real seconds, console.warn a structured dump: day, clock, activeEvent, every event status, and every actor's id, state, path length, and position. Expose window.g and window.world from beginDay for console debugging.

TASK 4 (REQUIRED): Add a dev-only movie mode to the shell. URL parameter ?movie=1 enables an autopilot: keeps the player at the desk, auto-picks choice index 2 on cards and clicks through the outcome, auto-wins fire drills, auto-clocks-in each day, and logs one line per day (day, standing, soul, money, tasks shipped). Completely inert without the flag. Verify the query param also works when ntos-standalone.html is opened from disk. This exists so a real rAF-driven tab can run for hours, which the headless soak cannot fully replicate.

TASK 5 (REQUIRED): Run persistence. Serialize g and runSeed to localStorage after closeDay, on clockIn, and after every encounter and crunch resolution. On load, if a save exists and the run is not over, show a Resume button on the start screen alongside the new-run CTA. Clear the save on game over and on new run. Add a game-test.js test that round-trips a mid-run g through JSON.stringify/parse and produces an identical subsequent career given the same remaining policy.

TASK 6 (REQUIRED, TIMEBOXED): Rebalance run length. The current win takes about 23 days at 3.6 real minutes per day, which is 80-plus minutes. Target a total winning run of 20 to 30 real minutes. Tune FU_TARGET (try 2500 first), and if needed CLOCK_SPEED, BURN_BASE, BURN_STEP, and ladder pay, until the third-way soak bot's median winning run lands in that window. One sweep, pick the constants, commit, move on. Keep the promotion tension intact: winning as a permanent Intern should remain nearly impossible, and the suck-up and rebel policy sims must still fail the way they do now. IMPORTANT: the existing policy-sim assertions in game-test.js encode the old balance (escape day 12 to 45, suck-up dead by day 6, and so on). Update those assertions deliberately to the new balance; do not treat them as untouchable, and do not delete them. Log the seed sweep results (days-to-escape distribution across 50 seeds) in SESSION_LOG.md with the final constants.

TASK 7 (STRETCH): Full touch support in the shell. Convert canvas input to pointer events: tap to move and pick, drag to pan, pinch to zoom. Make the crunch WORK button responsive to rapid taps without double-tap zoom interference (touch-action: manipulation). Make the floating HUD collapse or shrink below 420px viewport width so the canvas stays usable on phones. Mouse input must remain fully working; verify both before committing. If this breaks mouse play and the fix is not quick, revert and log it.

TASK 8 (STRETCH): Encounter depth. Four sub-items; treat each as independently committable, so if one fails, commit the others and log it. (a) In the shell, shuffle the display order of a card's choices each time it is shown while preserving correct mapping to effects (rules stay in ntos-game.js; the shuffle is presentation). (b) In ntos-game.js, make planDay prefer encounters not yet seen this run until the whole pool has been seen, then reset (seeded, deterministic, tested). (c) Fix the bradfoiled path: the current toast says you were sitting right there even when the player was away with an empty inbox; give that case its own signal or flag and its own line in the shell. (d) Add an off-screen threat indicator: when the boss or Brad is in patrol or raid state and outside the current viewport, draw an arrow at the screen edge pointing toward them (render code, so it lives in ntos-world.js render or the shell overlay, your call, but keep it out of step()).

TASK 9 (STRETCH): The walkout as a thing in the world. Add an EXIT door tile to FURNITURE. When canWalkOut becomes true mid-day, highlight the door and toast it. Clicking the door sends the player walking there; on arrival, emit a walkout signal, and the shell ends the run through the existing walkOut and verdict flow. Keep the day-end walkout button too. Test the signal in world-test.js.

TASK 10 (STRETCH): Share card. On the day-end and end screens, add a Share button that copies plain text to the clipboard: game name, day and title line, banked over target, standing, soul, one flavor line drawn from real run counters (times Brad stole, crunches survived, warnings received; add the counters to g so they persist and serialize), and a closing status line in the game's voice. No URLs in v1.

TASK 11 (STRETCH): Production polish in index.html. OG and Twitter card meta tags, description meta tag, inline SVG favicon. A small WebAudio sound layer (no asset files) with a mute toggle persisted to localStorage: one click for task shipped, one sting for boss catch, one for the fire drill. Everything must still work when ntos-standalone.html is opened as a plain file from disk with no server.

TASK 12 (REQUIRED AS THE FINAL STEP, whenever you stop): Final verification. Both test suites green, rebuild the standalone, rerun the 50-seed soak on the final build, and open the game once via the preview config to confirm zero console errors; if the preview is unavailable, log that explicitly. Then write the full Session 5 entry in SESSION_LOG.md (every change, every bug found and fixed, final balance numbers with the sweep, anything deliberately left undone and why) and update HANDOFF.md so the next session picks up cleanly.

Constraints: preserve the brain/world/shell separation strictly. Preserve determinism everywhere. Any new player-facing text matches the dry, bitter office-comedy voice of the existing cards. Do not add build tooling, frameworks, or dependencies beyond what exists. Do not touch the MARQUE folder. Commit after each task, do not push.
