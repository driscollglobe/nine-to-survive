You are working on Nine to Survive: Your Number, in /Users/kevindriscoll/Desktop/nine to survive/. Read SESSION_LOG.md and HANDOFF.md first. Session 5 shipped stability, persistence, rebalance to $2,500, touch input, EXIT door, share, and audio. This session tightens what exists. Structure is unchanged and must be preserved: ntos-game.js is the brain (no DOM, seeded mulberry32 on g.rngState only), ntos-world.js is the office (never touches meters, render is the only canvas code), index.html is the shell (presentation and wiring only), ntos-standalone.html is built by build_standalone.py and never hand-edited, tests run via osascript -l JavaScript under JavaScriptCore with no node or npm.
TASK 1 (REQUIRED): Reconcile HANDOFF.md. The Session 5 block at the top says $2,500, day 11 median, ladder 260 to 1000, clock 3.2. The body sections below it still say $6,000, Intern $180 to Director $800, escape day 23, 4 encounters a day, clock 2.2, and stale ?v= numbers. Rewrite the body so the entire document tells one current story. A future session recovering from this file must not find two conflicting truths.
TASK 2 (REQUIRED): Fix the determinism violation. The encounter choice display shuffle in the shell uses bare Math.random. Replace it with a deterministic presentation-only shuffle seeded from runSeed, g.day, and g.idxInDay. It must NOT advance g.rngState, since that would change career determinism. Add a test asserting that applying choices through a shuffled display still maps to the correct rule indices and that career state is byte-identical with and without the shuffle.
TASK 3 (REQUIRED): Movie mode should prove the escape path. Default ?movie=1 now walks out (through the EXIT door flow if mid-day, or the day-end button) as soon as canWalkOut is true, logging the full verdict. Add ?movie=greed which continues past the number until a terminal failure. Log which mode is active at start. Both must remain completely inert without the flag.
TASK 4 (REQUIRED): Anti-desk-camping, one small mechanic only. The desk-bot currently wins hollow without ever using the recovery loop. In the brain: if the player ships 3 tasks in a row without any Soul recovery action in between (coffee, couch, or chat resets the counter), each further consecutive task costs 1 extra Soul on top of the normal task cost. Name it in the day report and toast it once per day in the game's voice (something in the register of "dead-eyed productivity"). Keep it in the rules module, expose the counter on g so it serializes, test it, and confirm via the soak that the desk-bot now finishes with meaningfully lower Soul but the game remains winnable.
TASK 5 (REQUIRED, TIMEBOXED): Variance pass. All 50 seeds currently escape on exactly Day 11 because nothing random touches money under strong play. Add at most two seeded levers so the escape-day distribution for the strong policy spreads to roughly Day 10 to 14 with a median near 11 or 12. Candidate levers, pick the smallest: seeded daily task load (6 to 10 instead of always 8, with the day report and inbox reflecting it), and a cash bonus on crunch success (crunches are already seeded per day). Do not add new systems. One sweep, update the policy-sim assertions deliberately, log the before/after distribution across 50 seeds in SESSION_LOG.md, commit, stop tuning.
TASK 6 (STRETCH, the big one): Grow the encounter pool from 10 to 20. Ten new cards in the exact voice of the existing ones: dry, bitter, specific office-comedy, every choice a real tradeoff between Standing and Soul, the third-way option present but never free. Cover territory the pool lacks: the reply-all disaster, the performance calibration meeting, the reorg announcement, the consultant who interviews you about your own job, the farewell card for someone you never met, the "we're a family" all-hands, the expense report kicked back over $4, the open-plan noise war, the LinkedIn post a coworker writes about grinding, the exit interview invitation for someone else's departure. Each needs tag, clock, title, scene, three choices with s/so deltas in the established ranges, and an OWNER_BY_ENC owner. Keep the pool in ascending clock order; interleave new clocks between existing ones as needed since planDay relies on order. Update the content-integrity tests deliberately (the pool-size assertion says 10; make it 20) and confirm planDay's novelty cycle handles the larger pool. This is data plus tests only; no engine changes.
TASK 7 (REQUIRED AS THE FINAL STEP, whenever you stop): Rerun both suites and the 50-seed soak on the final build, rebuild the standalone, verify zero console errors via the preview if available (log it explicitly if not). Write the Session 6 entry in SESSION_LOG.md with every change, the new balance distribution, and anything left undone with reasoning. Update HANDOFF.md to match.
Constraints: preserve brain/world/shell separation strictly. Preserve determinism everywhere; after this session there must be zero bare Math.random calls in game code. New player-facing text matches the existing voice. No new tooling, frameworks, or dependencies. Do not touch the MARQUE folder. Commit after each task, do not push.

USE THESE FONTS ONLY:

The main site font looks like Poppins.
Likely breakdown:
Top logo text: Poppins Light / Regular, uppercase with loose letter spacing `NINE TO SURVIVE`
Hero headline: Poppins ExtraBold / Black “Merch, memes, and survival guides…”
Body copy: Poppins Regular “Nine to Survive is for the people…”
Logo badge text: Probably not the same font. It looks like a custom/cartoon display treatment, or something close to Luckiest Guy, Bowlby One SC or Cooper Black-adjacent. My guess: custom/AI-generated lettering, not a clean web font.
So for the site CSS, I’d start with:

```css
font-family: 'Poppins', sans-serif;

```

Weights: `300/400` for the nav, `800/900` for the headline.
